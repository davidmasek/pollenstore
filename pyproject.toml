[project]
name = "pollenstore"
version = "0.1.0"
description = "The best key-value store (and more) for bees 🐝."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = [
    "pyright>=1.1.407",
    "pytest>=8.4.2",
    "ruff>=0.14.4",
]
